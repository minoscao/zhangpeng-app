# Design QA — SKU 批量创作工作流

## 对照基准

- 产品库参考：老板标注的产品列表截图 `codex-clipboard-648e320b-2e26-43e3-8679-48565088a544.png`
- 批量工作台参考：老板提供的完整工作台示意 `codex-clipboard-0f4406c1-f794-4208-81b9-165eeaafa647.png`
- 同屏对照：`.impeccable/review/products-comparison.png`、`.impeccable/review/studio-comparison.png`
- 验收状态：产品库、产品抽屉、18 张批量计划、17 张保留结果和已回存产品库

## 视觉检查

- 产品库：通过。列表主图全部改为真实、干净的白底产品图，SKU、名称、品类、素材数、批次数和状态层级清晰。
- 产品详情：通过。桌面端使用右侧抽屉，移动端使用全宽抽屉；产品信息与已生成素材用两个标签页分离。
- 批量创作：通过。左侧配方、右侧生成计划符合参考的信息结构；参考图、组合摘要、公式和结果区各自成组。
- 结果分组：通过。素材按 SKU 分组，每个结果保留提示词标签，并提供单张重做与删除操作。
- 响应式：通过。390 × 844 手机视口无横向滚动，产品行、顶部按钮和抽屉均可点击；桌面密度不受影响。
- 可访问性：通过。核心按钮有可读名称，抽屉/弹窗可用 Escape 关闭并限制焦点，具有跳过主内容和减弱动画规则。

## 核心交互检查

- 点击产品整行打开右侧抽屉：通过。
- 产品信息 / 已生成素材标签页及标签筛选：通过。
- 从产品详情把 SKU 带入创作批次：通过。
- 多 SKU 参考图选择与移除：通过。
- 提示词组启用、编辑、数量设置、新增、删除：通过。
- 实时公式 `3 个产品 × 3 个地理位置 × 2 个颜色组 = 18 张素材`：通过。
- 生成前确认产品、数量、积分和预计时间：通过。
- 18 张预分配结果按 3 个 SKU 生成：通过。
- 单张重做、删除后保留 17 张结果：通过。
- 一键保存 17 张素材并回写对应 SKU 历史：通过。
- 刷新后保留本地批次和产品历史：通过。

## 修正记录

1. 首轮白底图路径在本地服务中不可访问；已让服务优先读取项目根素材并回退到应用素材目录。
2. 手机端产品表最初沿用桌面最小宽度；已改为四列核心信息并隐藏次要列，消除横向滚动。
3. 手机端顶部动作文字曾被 42px 图标按钮挤成竖排；已切换为带无障碍名称的纯图标显示。
4. 静态检查发现隐藏预览图缺少 `src`、布局属性动画和通用字体问题；核心反模式已全部清理。当前提示仅为既有样式尚未全部写入设计令牌文档的建议项。
5. 根据后续反馈移除创作页“固定产品规格”整段；流程收敛为“参考产品图 → 提示词组合 → 通用提示词”，桌面与手机端均已复查。

## 最终结果

**PASS** — P0：0，P1：0，P2：0。老板要求的 SKU 产品库、提示词组合、批量生成、分组结果和回存闭环已完成。
