# 本地开发专用。线上密钥必须用 `wrangler secret put QWEN_API_KEY` 保存。
QWEN_API_KEY=your_qwen_api_key_here

# Cloudflare Access 应用创建后填写。线上可作为普通 Worker 变量保存。
ACCESS_TEAM_DOMAIN=your_team_name
ACCESS_AUD=your_access_application_audience
