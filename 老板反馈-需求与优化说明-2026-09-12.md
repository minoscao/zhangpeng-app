# 老板新意见：需求整理与本轮优化

日期：2026-09-12。产品定位：面向儿童帐篷的真实产品参考图、国际市场素材批量创作工具。

## 核心结论

图片质量的第一标准是“符合需求”，不是单纯真实或漂亮。帐篷结构、产品颜色、指定背景和必须出现的元素需要同时落实；地域知识应由软件补充，而非要求操作人员逐个输入地标。

## 需求与本轮交付

| 老板意见 | 软件要求 | 本轮优化 |
| --- | --- | --- |
| Demo 缺少墨尔本或木屋等具体细节 | 将地点、背景与必须出现元素明确写入实际请求 | 新增墨尔本地域规则；城市天际线、木屋庭院等公共模板；补充要求拥有更高优先级 |
| 不应让人逐一填写当地地标 | 输入城市后，自动提供当地背景知识 | 内置地域库直接选用；新城市调用当前通道的文字 AI 推荐建筑、拍摄位置、住宅与景观，结果可查看和编辑 |
| 公共提示词区应有模板 | 可复用、可修改、可另存 | 新增需求优先、城市天际线、木屋庭院、亲子生活、白底电商五个公共模板，另存到当前浏览器 |
| 看不到批量出图结果 | 展示真实批次，不能用演示素材冒充 | 修复移除产品清空批次、结果依赖当前选择的问题；右侧单列大图；实际批次历史可切换 |
| 不确定是 Prompt 还是模型原因 | 同一产品、同一提示词的小样对比，记录配置 | 小样对比生成当前通道的全部模型配置，每个配置一张；显示具体型号与完整提示词；经用户确认后调用付费 API |
| 关注需求细节 | 结果需验收并可调整重做 | 增加结构、配色、地域、必须元素的人工验收标记；重做记录前次提示词与模型；归档保留完整提示词 |

## 已定位的问题

1. 移除参考产品原先会重置整个生成批次，现在只改变下一批的参考产品选择。
2. 结果列表原先只遍历当前选中产品，现在按批次真实结果和产品快照展示。
3. 请求原先读取正在编辑的公共提示词与比例，现在新批次在开始时保存产品、完整提示词、比例和模型配置快照，不会因中途改动而串配方。
4. 大图可能超过 localStorage 容量，原来的失败被静默忽略。现在增加 IndexedDB 图片与批次保存，失败会提醒下载备份。不是云端永久文件库，浏览器清理数据仍会丢失。
5. 旧背景提示存在“地标或住宅任选”的空间，城市地标可能被省略；新场景模板明确“天际线必须出现”或“小木屋必须出现”。白底模板排除地域背景，避免自相矛盾。

## 模型说明

- 保留 OpenAI GPT Image 2 / GPT Image 2.5 Sunburst，以及 Qwen Image 3.0 / Pro 配置。
- 增加官方已核对的 `wan2.6-image`（万相 2.6）参考图编辑配置，使用异步任务和已有查询流程；关闭提示词自动扩写，超过其 2000 字提示词上限时明确提示，不静默截断。
- 老板语音中的“万三”“3Q”“墨尔本审批”含义不明确。本轮按“墨尔本地域背景细节”和“模型小样对比”处理，没有把这些词当作精确的 API 型号或建筑审批需求。“万三”是否指万相 3，需要另行确认。
- 城市补全使用 OpenAI `gpt-4.1-mini` 或阿里 `qwen-plus` 文字模型，不是生图模型自己联网查地标；AI 推荐标记为未经联网核验。
- 阿里模型可用性取决于密钥所属地域、空间与权限。使用现有北京接入域名；跨地域密钥不保证可用。模型配置对比同时包含质量与扩写设置的差别，不能只凭一张结果证明某个模型更好。

## 建议的验收测试

1. 单个儿童帐篷 SKU，选择“澳大利亚·墨尔本”与“城市天际线”，先跑小样对比：检查墨尔本地域线索可辨识，不能出现悉尼歌剧院代替。
2. 保持同一 SKU 和配色，换“木屋自然庭院”：检查后方小木屋可辨识，帐篷结构完整。
3. 固定模型配置，分别用旧提示词与完善后的模板生成，评估提示词改进；再固定完善后的提示词测试各模型配置，尽量一次只改变一个变量。
4. 选择两个 SKU、两个地点、两个配色，核对 8 张批量结果，检查数量、失败原因、SKU 归属；改变参考产品选择后旧结果仍存在。
5. 放大检查面料、开口、支架与接触阴影，标记“符合需求”或“不符合需求”；不符合的结果修改要求后逐张重做。
6. 下载备份，刷新页面核对批次和已归档素材。阿里临时图片链接有时效，不能把浏览器保存链接等同于永久保存图片文件。

## 验证边界

本轮自动化测试使用模拟 API 响应，不调用用户历史消息中的密钥，不消耗真实生图额度。实际模型是否能稳定达到要求、哪个模型更好，仍需使用软件的小样对比进行真实付费测试；程序测试不能代替成片验收，也不承诺地标或产品结构百分之百准确。

官方参考：[OpenAI 图像生成](https://developers.openai.com/api/docs/guides/image-generation)、[GPT Image 2.5 Sunburst](https://developers.openai.com/api/docs/models/gpt-image-2.5-sunburst)、[万相 2.6 生成与编辑 API](https://www.alibabacloud.com/help/en/model-studio/wan-image-generation-api-reference)、[千问文字模型 API](https://www.alibabacloud.com/help/en/model-studio/qwen-api-via-openai-chat-completions)。
